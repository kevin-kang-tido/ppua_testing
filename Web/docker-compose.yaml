version: '3.8'
services:
  web:
    build: .
    restart: always
    expose:
      - "3000"
  nginx:
    image: nginx:alpine
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "80:80"
    depends_on:
      - web
